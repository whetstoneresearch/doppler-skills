{
  "name": "doppler-skills",
  "private": true,
  "packageManager": "bun@1.3.8",
  "engines": {
    "bun": ">=1.3.8"
  },
  "scripts": {
    "validate": "bun run validate:bun-version && bun run validate:syntax && bun run validate:skills && bun run validate:links",
    "validate:bun-version": "bun run scripts/check-bun-version.ts",
    "validate:syntax": "bun run scripts/validate-markdown-yaml.ts",
    "validate:skills": "bun run scripts/validate-skills.ts",
    "validate:links": "bun run scripts/check-links.ts"
  }
}
